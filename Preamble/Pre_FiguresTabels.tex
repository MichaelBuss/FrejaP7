% Use external image files (JPG, PNG, EPS, PDF)
\usepackage{graphicx}

% Base path for graphics
\graphicspath{{Graphics/}}

% Rotate text with \begin{sideways}...\end{sideways}
\usepackage[figuresright]{rotating}

% Color in tables with \columncolor and \rowcolor
\usepackage{colortbl}

% Define colors for reuse with \definecolor
\usepackage[table]{xcolor}

% Color palette that looks great
\definecolor{xRed}{HTML}{B51E0E}		%[0.71 0.12 0.06]
\definecolor{xGreen}{HTML}{6DB565}		%[0.23 0.51 0.20]
\definecolor{xBlue}{HTML}{074E82}		%[0.03 0.31 0.51]
\definecolor{xBrown}{HTML}{9C5C19}		%[0.61 0.36 0.10]
\definecolor{xYellow}{HTML}{F7B538}		%[0.97 0.71 0.22]
\definecolor{xOrange}{HTML}{EC6D00}		%[0.93 0.43 0.00]
\definecolor{xCyan}{HTML}{0094AC}		%[0.00 0.58 0.68]
\definecolor{xPurple}{HTML}{8711A1}		%[0.53 0.07 0.64]
\definecolor{xPink}{HTML}{D30580}		%[0.83 0.02 0.51]

%Grey scale (Linear)
\definecolor{xBlack}{HTML}{000000}		%[0.00 0.00 0.00]
\definecolor{xGreyDark}{HTML}{404040}	%[0.25 0.25 0.25]
\definecolor{xGreyMedium}{HTML}{7F7F7F}	%[0.50 0.50 0.50]
\definecolor{xGreyLight}{HTML}{BFBFBF}	%[0.75 0.75 0.75]
\definecolor{xWhite}{HTML}{FFFFFF}		%[1.00 1.00 1.00]

% Plots and draw vector graphics
\usepackage{pgfplots}
\usepackage{tikz-3dplot}
\usepackage{tikz}
\usetikzlibrary{decorations.markings, positioning, decorations, pgfplots.fillbetween, arrows.meta}

% Ensures floats always appear after their definition
\usepackage{flafter}

% Exact placement of floats with [H] ala \begin{figure}[H]
\let\newfloat\relax
\usepackage{float}

% Include other PDF documents with \includepdf[pages={x-y}]{filename.pdf}
\usepackage{pdfpages}

% Customise captions and enable subfigures
\usepackage{caption}
\usepackage{subcaption}
\captionsetup{labelfont={bf,it}}

% Common layouts for tabular column heads + Multi-lined tabular cells
\usepackage{makecell}

% Multi-column and multi-row cells in tables
\usepackage{multirow}

% Table columns that can fill remaining space
\usepackage{tabularx} % X column type that fills remaining space
\newcolumntype{R}{>{\raggedleft\arraybackslash}X}	% X, but right aligned
\newcolumntype{L}{>{\raggedright\arraybackslash}X}	% X, but left aligned
\newcolumntype{C}{>{\centering\arraybackslash}X}	% X, but center aligned

% Table that spans multiple pages
\usepackage{longtable}